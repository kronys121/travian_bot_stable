# Зафиксированные мажор.минор версии для воспроизводимости.
# Патч-версии обновляются автоматически (compatible release ~=).
playwright~=1.44
playwright-stealth~=1.0.6
beautifulsoup4~=4.12
requests~=2.31
PyYAML~=6.0
python-dotenv~=1.0
fastapi~=0.110
uvicorn~=0.29
PySocks~=1.7.1

# Десктопное GUI (gui.py). Веб-панели не требуется — можно не ставить,
# если пользуешься только браузерной версией.
customtkinter~=5.2
